<template>
	<descriptor
		id="org.springframework.data.gemfire.template.server.standalone"
		name="Spring Data GemFire Cache Server Project (Standalone)"
		kind="template"
		category="${template.category}"
		requiresbundle="${template.requires.bundles}"
		version="${template.version}">
		<description>
			Creates a Gemfire Cache Server project with a cache server, embedded cache and one region that runs as a standalone Java
			application.
		</description>
	</descriptor>
	<project path="" />
	<json path="wizard.json" />
</template>
