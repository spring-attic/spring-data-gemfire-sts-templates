<template>
	<descriptor
		id="org.springframework.data.gemfire.template.standalone"
		name="Spring Data GemFire Project (Standalone)"
		kind="template"
		category="${template.category}"
		requiresbundle="${template.requires.bundles}"
		version="${template.version}">
		<description>
			Creates a Spring Data GemFire project with an embedded cache and one region that runs as a standalone Java
			application.
		</description>
	</descriptor>
	<project path="" />
	<json path="wizard.json" />
</template>
